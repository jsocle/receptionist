buildscript {
    ext.kotlin_version = '0.14.449'
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"
    }
}

apply plugin: 'kotlin'

repositories {
    mavenCentral()
    maven {
        url "https://jitpack.io"
    }
}

dependencies {
    compile "org.jetbrains.kotlin:kotlin-stdlib:$kotlin_version"
    compile 'com.github.jsocle:jsocle:0.0.13'
    compile 'com.github.jsocle:jscole-hibernate:0.0.3'
    compile 'com.h2database:h2:1.4.189'

    testCompile 'junit:junit:4.12'
}

sourceSets {
    main.java.srcDirs += 'src/main/kotlin'
}

apply plugin: 'application'
//noinspection GroovyUnusedAssignment
applicationName = 'app'
//noinspection GroovyUnusedAssignment
mainClassName = 'com.github.jsocle.receptionist.ReceptionistPackage'


task stage(dependsOn: ['clean', 'installApp'])
